#include <stdio.h>

int main()
{
    int num;
    int mask=0x1;
    printf("enter number: ");
    scanf("%d",&num);
    if((num & mask)==0)
    {
        printf("number is even");
    }
    else
    {
        printf("number is odd");
    }

    return 0;
}
