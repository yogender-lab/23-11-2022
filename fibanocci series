
#include <stdio.h>

int main()
{
   int x=0,y=1,z,n;
   printf("enter number of terms:");
   scanf("%d",&n);
   for(int i=0;i<n;i++)
   {
       z=x+y;
       printf("%d\t",z);
       x=y;
       y=z;
   }
    return 0;
}
