#include <stdio.h>

int main()
{
    int num;
    printf("enter number:");
    scanf("%d",&num);
    for(int i=31;i>=0;i--)
    {
        if(1&(num>>i))
        {
            printf("1");
        }
        else
        {
            printf("0");
        }
    }

    return 0;
}
